# Custom Type Demo

_Just because you can, doesn't mean you should!_

This sample demonstrates the flexibility of the system by defining a custom type which is a
running web server. The type handler recurses into the server struct having decorated the
key store to apply a prefix to the keys.
